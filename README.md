# WARNING: DON'T RUN NPM INSTALL INSIDE THIS DIRECTORY, FOLLOW THESE STEPS BELOW
# Steps to build and run

1. npm install --global yarn
2. npm install --global gulp
3. yarn install
4. gulp build
5. gulp

# Based on TailAdmin
